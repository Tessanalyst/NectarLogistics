{
  "name": "vercel-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild index.ts --bundle --platform=node --format=esm --outdir=dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "esbuild": "^0.19.9",
    "typescript": "^5.4.0"
  },
  "type": "module"
}
